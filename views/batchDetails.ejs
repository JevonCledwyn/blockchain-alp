<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Get Batch Details</title>
    <link
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f7f7f7;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      .container {
        background-color: #ffffff;
        padding: 40px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        max-width: 600px;
      }
      h3 {
        color: #333333;
        margin-bottom: 20px;
      }
      ul {
        list-style-type: none;
        padding: 0;
      }
      ul li {
        background-color: #f0e4d7;
        margin-bottom: 10px;
        padding: 15px;
        border-radius: 5px;
        border: 1px solid #ddd;
      }
      ul li strong {
        color: #555555;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <% if (details) { %>
      <h3 class="text-center">Batch Details</h3>
      <ul>
        <li><strong>ID:</strong> <%= details[0] %></li>
        <li><strong>Details:</strong> <%= details[1] %></li>
        <li><strong>Owner:</strong> <%= details[2] %></li>
        <li><strong>State:</strong> <%= details[3] %></li>
        <li><strong>History:</strong> <%= details[4].join(', ') %></li>
      </ul>
      <% } %>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </body>
</html>
